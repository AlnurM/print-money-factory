---
phase: 08-debug-cycle-memory
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - workflows/discuss.md
autonomous: true
requirements: [DBUG-02, DBUG-03]

must_haves:
  truths:
    - "Running /brrr:discuss in debug mode displays a Prior Debug Cycles summary table with failed approaches before gathering new decisions"
    - "The AI uses cumulative do_not_retry entries to constrain its suggestions -- it must not propose parameter ranges that overlap with any prior do_not_retry entry"
    - "Debug memory is scoped per milestone -- only phase_*_diagnosis.json files in .pmf/phases/ are read"
    - "When total failed_approaches entries across all diagnosis files exceed 50, oldest entries are merged into a compact summary rather than dropped"
  artifacts:
    - path: "workflows/discuss.md"
      provides: "Step 1 reads diagnosis JSONs, Step 2-debug presents failure table and enforces do_not_retry constraints"
      contains: "diagnosis.json"
  key_links:
    - from: "workflows/discuss.md"
      to: ".pmf/phases/phase_*_diagnosis.json"
      via: "Glob + Read in Step 1 and Step 2-debug"
      pattern: "diagnosis\\.json"
    - from: "workflows/discuss.md Step 2-debug"
      to: "do_not_retry entries"
      via: "Constraint enforcement during suggestion generation"
      pattern: "do_not_retry"
---

<objective>
Add debug memory consumption to the discuss workflow so it reads prior diagnosis artifacts and presents failures before gathering new decisions.

Purpose: The discuss workflow in debug-discuss mode must read ALL `phase_*_diagnosis.json` files, present a summary table of prior failed approaches, and constrain the AI's suggestions to avoid retrying anything in the cumulative `do_not_retry` list. Also enforce a 50-entry cap with merge-not-evict semantics.

Output: Modified `workflows/discuss.md` with diagnosis JSON reading in Step 1 and failure summary presentation + constraint enforcement in Step 2-debug.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-debug-cycle-memory/08-CONTEXT.md
@.planning/phases/08-debug-cycle-memory/08-01-PLAN.md

<interfaces>
<!-- Plan 01 creates the diagnosis JSON with this structure (from D-02): -->
<!--
{
  "phase": N,
  "timestamp": "ISO-8601",
  "strategy_type": "SMA crossover",
  "best_metrics": { "sharpe": 0.8, "max_dd": -0.15 },
  "targets": { "sharpe": 1.5, "max_dd": -0.10 },
  "failed_approaches": [
    {
      "iteration_range": "1-10",
      "params_tried": { "fast_period": [5,10,15], "slow_period": [20,30,50] },
      "best_result": { "sharpe": 0.8, "trades": 45 },
      "diagnosis": "Entries too frequent in sideways markets",
      "do_not_retry": ["fast_period < 10 with slow_period < 30", "stop_loss < 1.5%"]
    }
  ],
  "overall_diagnosis": "Strategy shows edge in trending markets...",
  "suggested_changes": ["Widen stop-loss to 2-3%", ...]
}
-->

<!-- discuss.md current structure relevant to this plan: -->
<!-- Step 1: Load Strategy Context (line 197) - reads STRATEGY.md, STATE.md, prior phase artifacts -->
<!-- Step 2-debug: Strategy Discussion debug-discuss mode (line 354) - AI diagnosis from prior phase data -->
<!-- Mode Detection preamble (line 177) - determines first-discuss vs debug-discuss vs auto -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add diagnosis JSON reading to discuss.md Step 1 and failure table to Step 2-debug</name>
  <files>workflows/discuss.md</files>
  <read_first>
    - workflows/discuss.md (full file -- focus on Step 1 starting at line 197 and Step 2-debug starting at line 354)
    - .planning/phases/08-debug-cycle-memory/08-CONTEXT.md (decisions D-05 through D-10)
  </read_first>
  <action>
Make two modifications to workflows/discuss.md:

**Modification 1: Step 1 (Load Strategy Context) -- add diagnosis JSON reading**

In Step 1 item 4 (the debug-discuss block that reads prior phase artifacts), add after the existing artifact reads (discuss, research, plan, best_result, verify report):

```
   - Read ALL diagnosis JSON files: Use Glob to find `.pmf/phases/phase_*_diagnosis.json`. Read each one and collect:
     - All `failed_approaches` entries across all files
     - All `do_not_retry` entries (flattened from all failed_approaches across all files)
     - The `overall_diagnosis` from the most recent diagnosis file
     - The `suggested_changes` from the most recent diagnosis file
   - Count total `failed_approaches` entries across all diagnosis files
   - If total exceeds 50 (per DBUG-03), merge the oldest entries:
     - Take the oldest N entries that bring the total down to 45 (leaving room for growth)
     - Replace them with a single summary entry:
       ```json
       {
         "iteration_range": "merged (phases 1-M)",
         "params_tried": {},
         "best_result": { "sharpe": best_sharpe_from_merged, "trades": total_trades_from_merged },
         "diagnosis": "Early phases summary: [AI-generated 2-3 sentence summary of the merged entries]",
         "do_not_retry": [all unique do_not_retry entries from merged entries, deduplicated]
       }
       ```
     - Write the updated diagnosis files back (remove entries from oldest files, add merged entry to earliest remaining file)
   - Store the collected data for use in Step 2-debug:
     - `all_failed_approaches`: the full list (after any merging)
     - `all_do_not_retry`: deduplicated flat list of all do_not_retry strings
     - `latest_diagnosis`: overall_diagnosis from most recent file
     - `latest_suggestions`: suggested_changes from most recent file
```

Add to the "Summarize what you found" display block (after the existing lines about prior phases and last phase metrics):
```
     Debug memory: {count} failed approaches across {file_count} diagnosis files
     Do-not-retry constraints: {count} active
```

**Modification 2: Step 2-debug (Strategy Discussion debug-discuss mode) -- add failure summary table and constraint enforcement**

Insert a new numbered item after item 1 (the existing diagnosis formulation from prior phase artifacts) and before item 2 (the "Present the diagnosis to the user" block). This new item presents the debug memory:

```
2. **Present prior debug cycle memory** (DBUG-02):

   If diagnosis JSON files were loaded in Step 1, present a failure summary table BEFORE the diagnosis:

   ```
   ## Prior Debug Cycles

   | Phase | Iterations | Best Sharpe | Diagnosis | Do NOT Retry |
   |-------|------------|-------------|-----------|--------------|
   | {phase} | {iteration_range} | {best_sharpe} | {diagnosis (truncated to 60 chars)} | {do_not_retry entries, comma-separated} |
   | ...   | ...        | ...         | ...       | ...          |

   Active constraints ({count} total):
   {bulleted list of ALL unique do_not_retry entries across all phases}

   AI synthesis: {latest_overall_diagnosis from most recent diagnosis file}
   Suggested starting point: {latest_suggested_changes, bulleted}
   ```

   If no diagnosis JSON files were found, skip this presentation and proceed with the existing diagnosis flow.
```

Renumber the existing items 2-5 to 3-6 (shift all by 1).

In the renumbered item 3 (previously item 2, "Present the diagnosis to the user"), add this instruction at the end:

```
   - **CRITICAL CONSTRAINT (DBUG-02):** When formulating the diagnosis and suggested changes, you MUST NOT propose any parameter ranges or approaches that overlap with entries in the `all_do_not_retry` list from Step 1. If your analysis suggests a parameter region that is in the do_not_retry list, explicitly state: "This region was already tried in Phase {N} and failed -- skipping." Choose an alternative direction instead.
```

In the renumbered item 4 (previously item 3, "Let the user react and discuss"), add:

```
   - If the user proposes a change that overlaps with a do_not_retry entry, warn them: "That approach was tried in Phase {N} and failed because: {diagnosis}. Are you sure you want to retry it?" Let the user override if they insist, but make the prior failure visible.
```
  </action>
  <verify>
    <automated>grep -c "diagnosis.json" workflows/discuss.md | grep -q "[2-9]" && grep -q "do_not_retry" workflows/discuss.md && grep -q "DBUG-02" workflows/discuss.md && grep -q "DBUG-03" workflows/discuss.md && grep -q "Prior Debug Cycles" workflows/discuss.md && grep -q "exceeds 50" workflows/discuss.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/discuss.md Step 1 item 4 includes Glob for `.pmf/phases/phase_*_diagnosis.json` and reads all files
    - Step 1 includes the 50-entry cap logic with merge-not-evict semantics (per D-09, DBUG-03)
    - Step 1 summary display shows debug memory count and do-not-retry constraint count
    - Step 2-debug contains a "Prior Debug Cycles" table presentation (per D-06)
    - Step 2-debug contains "Active constraints" list of all do_not_retry entries
    - Step 2-debug contains "AI synthesis" from latest overall_diagnosis
    - The CRITICAL CONSTRAINT block references DBUG-02 and prohibits proposing parameter regions in do_not_retry
    - User override warning exists for when users propose retrying a failed approach
    - Memory scoping is per-milestone via the `.pmf/phases/` directory (per D-08) -- no cross-milestone reads
    - grep for "DBUG-02" and "DBUG-03" both find matches in the file
  </acceptance_criteria>
  <done>discuss.md reads all diagnosis JSONs in Step 1 (with 50-entry cap), presents a Prior Debug Cycles failure table in Step 2-debug, and enforces do_not_retry constraints on AI suggestions</done>
</task>

</tasks>

<verification>
- `grep -q "phase_.*_diagnosis.json" workflows/discuss.md` -- file references diagnosis artifacts
- `grep -q "do_not_retry" workflows/discuss.md` -- constraint enforcement is documented
- `grep -q "Prior Debug Cycles" workflows/discuss.md` -- failure table heading exists
- `grep -q "exceeds 50" workflows/discuss.md` or `grep -q "exceed 50" workflows/discuss.md` -- size cap logic exists
- `grep -q "DBUG-02" workflows/discuss.md` -- requirement ID tagged
- `grep -q "DBUG-03" workflows/discuss.md` -- requirement ID tagged
</verification>

<success_criteria>
- The discuss workflow in debug-discuss mode reads ALL prior diagnosis JSONs and presents failures as a table before gathering new decisions
- The AI is constrained from suggesting parameter regions that overlap with do_not_retry entries
- Debug memory is scoped per milestone (reads from .pmf/phases/ only)
- When total failed_approaches exceed 50, oldest entries are merged into a compact summary (not dropped)
- Users who insist on retrying a failed approach are warned but can override
</success_criteria>

<output>
After completion, create `.planning/phases/08-debug-cycle-memory/08-02-SUMMARY.md`
</output>
