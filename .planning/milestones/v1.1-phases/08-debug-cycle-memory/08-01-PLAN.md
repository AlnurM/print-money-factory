---
phase: 08-debug-cycle-memory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/verify.md
autonomous: true
requirements: [DBUG-01]

must_haves:
  truths:
    - "After /brrr:verify --debug, a phase_N_diagnosis.json file exists in .pmf/phases/ with failed_approaches and do_not_retry entries"
    - "The diagnosis JSON is AI-generated from iteration artifacts, not a mechanical dump"
    - "Each diagnosis file is append-only -- running verify --debug again on the same phase appends to the existing file"
  artifacts:
    - path: "workflows/verify.md"
      provides: "Step 5b writes phase_N_diagnosis.json alongside debug_diagnosis.md"
      contains: "phase_N_diagnosis.json"
  key_links:
    - from: "workflows/verify.md"
      to: ".pmf/phases/phase_N_diagnosis.json"
      via: "JSON write in Step 5b"
      pattern: "diagnosis\\.json"
---

<objective>
Add structured diagnosis JSON artifact to the verify --debug workflow path.

Purpose: When a debug cycle completes, the AI must write a machine-readable `phase_N_diagnosis.json` with failed approaches, parameter regions that did not work, and explicit "do NOT retry" entries. This gives the discuss workflow (Plan 02) structured data to prevent retrying failed approaches.

Output: Modified `workflows/verify.md` with a new sub-step in Step 5b that writes the diagnosis JSON.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-debug-cycle-memory/08-CONTEXT.md

<interfaces>
<!-- The executor needs to understand the existing Step 5b structure in verify.md -->
<!-- Step 5b currently has three sub-steps: -->
<!-- 5b.1: Write Debug Diagnosis (debug_diagnosis.md) - reads iteration artifacts, writes markdown diagnosis -->
<!-- 5b.2: Update STATE.md for New Phase Cycle - increments phase, adds checklist -->
<!-- 5b.3: Display Debug Output - shows summary to user -->
<!-- The new diagnosis JSON step goes between 5b.1 and 5b.2 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add diagnosis JSON write step to verify.md Step 5b</name>
  <files>workflows/verify.md</files>
  <read_first>
    - workflows/verify.md (full file -- focus on Step 5b starting at line 812)
    - .planning/phases/08-debug-cycle-memory/08-CONTEXT.md (decisions D-01 through D-04, D-08, D-10)
  </read_first>
  <action>
Insert a new sub-step **5b.1.5** (between existing 5b.1 and 5b.2) in workflows/verify.md titled "Write Diagnosis JSON (DBUG-01)".

This step instructs the AI to write a `phase_N_diagnosis.json` file to `.pmf/phases/`. The instructions must cover:

1. **Check for existing diagnosis file**: Read `.pmf/phases/phase_N_diagnosis.json` if it exists (per D-10, this is append-only -- a phase may have multiple debug cycles). If it exists, parse the existing `failed_approaches` array. If it does not exist, start with an empty array.

2. **Build a new failed_approaches entry** from the data already loaded in 5b.1 (iteration verdicts, metrics, params, best_result). The entry must contain:
   - `iteration_range`: string like "1-10" covering iterations in this phase cycle
   - `params_tried`: object mapping each parameter name to the array of distinct values tried across iterations
   - `best_result`: object with the best metrics from this cycle (sharpe, max_dd, trades, win_rate)
   - `diagnosis`: string -- the AI's 1-2 sentence diagnosis of why this approach failed (same analysis as debug_diagnosis.md but condensed)
   - `do_not_retry`: array of strings -- explicit parameter regions or approaches the AI determines should NOT be retried. Examples: "fast_period < 10 with slow_period < 30", "stop_loss < 1.5%", "mean-reversion without volume filter". The AI must formulate these based on which parameter regions consistently underperformed.

3. **Append** the new entry to the `failed_approaches` array.

4. **Assemble the full JSON object** with this structure (per D-02):
```json
{
  "phase": N,
  "timestamp": "ISO-8601 timestamp",
  "strategy_type": "from STRATEGY.md",
  "best_metrics": { "sharpe": X, "max_dd": X, ... },
  "targets": { "sharpe": X, "max_dd": X, ... },
  "failed_approaches": [ ...existing entries..., new_entry ],
  "overall_diagnosis": "AI synthesis of ALL failed approaches so far",
  "suggested_changes": ["specific change 1", "specific change 2", ...]
}
```

5. **Write** the JSON to `.pmf/phases/phase_N_diagnosis.json` using the Write tool. If the file already existed, the `phase`, `strategy_type`, and `targets` fields stay the same. The `timestamp`, `best_metrics`, `overall_diagnosis`, and `suggested_changes` are updated to reflect the latest cycle.

6. **Display** confirmation: `Diagnosis artifact written: .pmf/phases/phase_N_diagnosis.json ({M} failed approaches recorded)`

Add this step as a new markdown section with the same formatting style as the surrounding 5b sub-steps (### heading, numbered instructions, code blocks for examples).

Renumber 5b.2 to 5b.3 and 5b.3 to 5b.4. Update the reference in the last line of the file ("skip Step 6 -- STATE.md already updated in 5b.3" instead of "5b.2").

Also update the Step 5b.4 display output (previously 5b.3) to mention the diagnosis JSON alongside the debug_diagnosis.md:
```
Debug Diagnosis for Phase {N}:

{Brief summary}

Diagnosis saved to:
  .pmf/phases/phase_{N}_verify/debug_diagnosis.md (human-readable)
  .pmf/phases/phase_{N}_diagnosis.json (structured, {M} failed approaches)

New phase cycle opened. Run /brrr:discuss to start from the diagnosis.
```
  </action>
  <verify>
    <automated>grep -c "diagnosis.json" workflows/verify.md | grep -q "[2-9]" && grep -q "do_not_retry" workflows/verify.md && grep -q "failed_approaches" workflows/verify.md && grep -q "DBUG-01" workflows/verify.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - workflows/verify.md contains a new sub-step between 5b.1 and 5b.2 with heading containing "Diagnosis JSON" and "DBUG-01"
    - The step includes the exact JSON structure from D-02 with fields: phase, timestamp, strategy_type, best_metrics, targets, failed_approaches, overall_diagnosis, suggested_changes
    - Each failed_approaches entry includes: iteration_range, params_tried, best_result, diagnosis, do_not_retry (per D-03)
    - Instructions specify checking for existing diagnosis file and appending (per D-10)
    - The AI is instructed to formulate diagnosis and do_not_retry from iteration data (per D-04), not mechanically dump
    - Former 5b.2 and 5b.3 are renumbered to 5b.3 and 5b.4
    - The display output in 5b.4 mentions both debug_diagnosis.md and diagnosis.json
  </acceptance_criteria>
  <done>verify.md Step 5b contains a new sub-step that writes phase_N_diagnosis.json with the D-02 JSON structure, append-only semantics, and AI-generated do_not_retry entries</done>
</task>

</tasks>

<verification>
- `grep -q "phase_N_diagnosis.json" workflows/verify.md` -- file references the new artifact
- `grep -q "do_not_retry" workflows/verify.md` -- do_not_retry field is documented
- `grep -q "failed_approaches" workflows/verify.md` -- failed_approaches array is documented
- `grep -q "DBUG-01" workflows/verify.md` -- requirement ID tagged
- The step appears between 5b.1 (debug_diagnosis.md) and the STATE.md update step
</verification>

<success_criteria>
- The verify --debug workflow path now writes a structured diagnosis JSON in addition to the existing markdown diagnosis
- The JSON follows the exact schema from D-02
- Append-only semantics per D-10 are instructed
- The AI is directed to generate do_not_retry entries from analysis, not mechanical extraction
</success_criteria>

<output>
After completion, create `.planning/phases/08-debug-cycle-memory/08-01-SUMMARY.md`
</output>
