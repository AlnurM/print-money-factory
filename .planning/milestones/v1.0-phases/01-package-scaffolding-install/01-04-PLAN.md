---
phase: 01-package-scaffolding-install
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified: []
autonomous: false
requirements: [INST-01, INST-02, INST-03, INST-04, INST-05, ARCH-01, ARCH-02, ARCH-03, ARCH-04, ARCH-05]

must_haves:
  truths:
    - "npx print-money-factory install completes successfully end-to-end"
    - "All 8 command files exist in ~/.claude/commands/brrr/ after install"
    - "~/.pmf/venv/bin/python can import pandas, numpy, ccxt, yfinance, plotly, ta, matplotlib, optuna"
    - "pytest runs against metrics test_metrics.py and all tests pass"
    - "Running install a second time completes without errors"
    - "~/.pmf/.version file contains correct version number"
  artifacts:
    - path: "~/.claude/commands/brrr/execute.md"
      provides: "Installed command file"
      contains: "brrr:execute"
    - path: "~/.pmf/venv/bin/python"
      provides: "Python venv binary"
    - path: "~/.pmf/references/metrics.py"
      provides: "Installed metrics module"
      contains: "compute_all_metrics"
  key_links:
    - from: "bin/install.mjs"
      to: "~/.claude/commands/brrr/"
      via: "file copy during install"
      pattern: "commands"
    - from: "bin/install.mjs"
      to: "~/.pmf/venv/"
      via: "python3 -m venv creation"
      pattern: "venv"
---

<objective>
Run the full install script end-to-end, verify all files are in place, run metrics tests, and confirm idempotent reinstall works. This is the integration test for the entire phase.

Purpose: Validates that Plans 01-03 produce a working installable package. Catches any wiring issues between the install script, file structure, and Python venv.
Output: Verified working installation with all tests passing.
</objective>

<execution_context>
@/Users/alikeforalike/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alikeforalike/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-package-scaffolding-install/01-01-SUMMARY.md
@.planning/phases/01-package-scaffolding-install/01-02-SUMMARY.md
@.planning/phases/01-package-scaffolding-install/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run install script and verify complete installation</name>
  <files></files>
  <read_first>
    bin/install.mjs
    package.json
    requirements.txt
  </read_first>
  <action>
Run the install script from the project root and verify all outputs.

**Step 1: Run install**
```bash
node bin/install.mjs install
```
Expect: successful completion with [OK] messages for each step.

**Step 2: Verify command files installed**
```bash
ls -la ~/.claude/commands/brrr/
```
Expect: 8 .md files (new-milestone.md, discuss.md, research.md, plan.md, execute.md, verify.md, status.md, update.md)

**Step 3: Verify PMF directory structure**
```bash
ls -la ~/.pmf/
ls -la ~/.pmf/workflows/
ls -la ~/.pmf/templates/
ls -la ~/.pmf/references/
ls ~/.pmf/references/pinescript-examples/
```
Expect: all directories exist with correct files

**Step 4: Verify version file**
```bash
cat ~/.pmf/.version
```
Expect: JSON with version matching package.json, installed date, package name

**Step 5: Verify Python venv and dependencies**
```bash
~/.pmf/venv/bin/python -c "import pandas; import numpy; import ccxt; import yfinance; import plotly; import ta; import matplotlib; import optuna; print('All imports OK')"
```
Expect: "All imports OK"

**Step 6: Run metrics tests**
```bash
cd ~/.pmf/references && ~/.pmf/venv/bin/python -m pytest test_metrics.py -v
```
Expect: All tests pass

**Step 7: Verify idempotent reinstall**
```bash
node bin/install.mjs install
```
Expect: completes without errors, "Updating existing venv..." message appears

**Step 8: Verify files not corrupted after reinstall**
```bash
ls ~/.claude/commands/brrr/*.md | wc -l
~/.pmf/venv/bin/python -c "import pandas; print('Still OK')"
```
Expect: 8 files, imports still work

If any step fails, diagnose and fix the issue in the source files (bin/install.mjs, package.json, etc.), then re-run.
  </action>
  <verify>
    <automated>ls ~/.claude/commands/brrr/*.md | wc -l | tr -d ' ' | grep -q "^8$" && ~/.pmf/venv/bin/python -c "import pandas; import numpy; import ccxt; import yfinance; import plotly; import ta; import matplotlib; import optuna; print('OK')" && cd ~/.pmf/references && ~/.pmf/venv/bin/python -m pytest test_metrics.py -x -q && echo "INTEGRATION TEST PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - `ls ~/.claude/commands/brrr/*.md | wc -l` outputs 8
    - `ls ~/.pmf/workflows/*.md | wc -l` outputs 7
    - `ls ~/.pmf/templates/` shows STRATEGY.md, STATE.md, pinescript-template.pine, report-template.html
    - `ls ~/.pmf/references/` shows metrics.py, test_metrics.py, conftest.py, backtest_engine.py, backtest-engine.md, data_sources.py, common-pitfalls.md
    - `ls ~/.pmf/references/pinescript-examples/` shows 3 .pine files
    - `cat ~/.pmf/.version | python3 -c "import json,sys; d=json.load(sys.stdin); assert 'version' in d"` exits 0
    - `~/.pmf/venv/bin/python -c "import pandas, numpy, ccxt, yfinance, plotly, ta, matplotlib, optuna"` exits 0
    - `cd ~/.pmf/references && ~/.pmf/venv/bin/python -m pytest test_metrics.py -x -q` exits 0
    - Running `node bin/install.mjs install` a second time exits 0
  </acceptance_criteria>
  <done>Full install completes successfully, all files in correct locations, Python venv has all dependencies, metrics tests pass, idempotent reinstall works.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of installed commands in Claude Code</name>
  <files></files>
  <action>
Human verifies that the installed package works correctly in a real Claude Code environment. This cannot be automated because it requires verifying Claude Code's command discovery system recognizes the installed /brrr:* commands.
  </action>
  <what-built>Complete Phase 1 installation: npm package that copies slash commands to ~/.claude/commands/brrr/, installs workflows/templates/references to ~/.pmf/, creates Python venv with all backtest dependencies, includes tested metrics module and reference patterns.</what-built>
  <how-to-verify>
    1. Open a new Claude Code session (or refresh command list)
    2. Type `/brrr:` and verify autocomplete shows: new-milestone, discuss, research, plan, execute, verify, status, update
    3. Run `/brrr:status` -- should load the status workflow (even though it is a stub, it should be recognized as a command)
    4. In terminal, run: `~/.pmf/venv/bin/python -c "import pandas; print(pandas.__version__)"` -- should print pandas version
    5. In terminal, run: `cd ~/.pmf/references && ~/.pmf/venv/bin/python -m pytest test_metrics.py -v` -- should show all tests passing with green output
    6. Check `~/.pmf/references/backtest_engine.py` -- verify it has clear anti-lookahead comments
  </how-to-verify>
  <verify>Human confirms all 6 verification steps pass</verify>
  <acceptance_criteria>
    - Claude Code recognizes `/brrr:` commands (autocomplete shows 8 commands)
    - `/brrr:status` loads without error
    - Python venv reports pandas version successfully
    - All metrics tests show green output
  </acceptance_criteria>
  <done>Human has confirmed all /brrr:* commands appear in Claude Code and the installation is fully functional.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Full install completes with exit code 0
- 8 command files in ~/.claude/commands/brrr/
- All Python dependencies importable
- Metrics tests all pass
- Idempotent reinstall works
- Human verifies commands appear in Claude Code
</verification>

<success_criteria>
- Install script works end-to-end on a real system
- All Phase 1 success criteria from ROADMAP.md are met:
  1. npx install copies slash commands (verified by file count)
  2. Second install is safe (verified by idempotent test)
  3. Python venv has all dependencies (verified by import test)
  4. Python 3.10+ detection works (verified by install script logic)
  5. Package has correct directory structure with tested metrics module (verified by pytest)
</success_criteria>

<output>
After completion, create `.planning/phases/01-package-scaffolding-install/01-04-SUMMARY.md`
</output>
