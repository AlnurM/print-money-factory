---
phase: 03-strategy-specification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/research.md
autonomous: true
requirements: [RSCH-01, RSCH-02, RSCH-03, RSCH-04, RSCH-05]

must_haves:
  truths:
    - "User runs /brrr:research and gets known implementations, academic references, and lookahead trap warnings for their strategy type"
    - "User runs /brrr:research --deep and gets extended search with 3-5 sources and deeper analysis per source"
    - "System auto-recommends whether research is needed based on strategy complexity"
    - "Output is phase_N_research.md with implementations, pitfalls, and formalization alternatives"
  artifacts:
    - path: "workflows/research.md"
      provides: "Complete behavioral workflow for /brrr:research command"
      min_lines: 150
      contains: "phase_N_research.md"
  key_links:
    - from: "workflows/research.md"
      to: ".pmf/phases/phase_N_discuss.md"
      via: "Read tool to understand strategy decisions"
      pattern: "phase_N_discuss"
    - from: "workflows/research.md"
      to: ".pmf/STATE.md"
      via: "Read for validation, Write for step update"
      pattern: "STATE.md"
    - from: "workflows/research.md"
      to: "references/common-pitfalls.md"
      via: "Read tool for cross-referencing known pitfalls"
      pattern: "common-pitfalls"
---

<objective>
Replace the research workflow stub with a complete behavioral workflow that finds known implementations, academic work, and lookahead traps for the user's strategy type, supports --deep mode for extended research, and auto-recommends whether research is needed.

Purpose: Research bridges discuss and plan -- it catches pitfalls before the plan is designed and surfaces formalization alternatives the user may not know about. It is optional but the system recommends it for non-standard strategies.

Output: `workflows/research.md` -- complete behavioral workflow (~200-280 lines)
</objective>

<execution_context>
@/Users/alikeforalike/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alikeforalike/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strategy-specification/03-CONTEXT.md

<interfaces>
<!-- Established workflow pattern from Phase 2 -->

From workflows/new-milestone.md (canonical structure):
- Preamble sections for validation and context scan
- ## Step N: Title with numbered substeps
- Behavioral instructions Claude reads and executes
- Tools: Read, Write, Bash, Glob (research also gets WebFetch)
- Footer: *Workflow: name* and *Covers: REQ-01, ...*

From commands/research.md (command that @-references this workflow):
```yaml
allowed-tools: [Read, Write, Bash, Glob, WebFetch]
```
Note: research is the ONLY command with WebFetch -- it can fetch web pages.

From references/common-pitfalls.md (cross-reference during research):
- 6 pitfalls cataloged: Lookahead Bias, Survivorship Bias, Execution Price Bias, Over-Optimization, Commission Omission, IS/OOS Confusion
- Each has: Description, Example, Detection, Prevention
- Research should cross-reference strategy type against these pitfalls

Expected input artifact (from discuss workflow, Plan 01):
```markdown
# Phase {N} -- Strategy Discussion
## Entry Logic / Exit Logic / Stop-Loss / Take-Profit
## Parameters (to optimize and fixed)
```

Sequence validation (from new-milestone.md validation matrix):
```
| research | Phase N discuss done (research optional) | .pmf/phases/phase_N_discuss.md exists |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace research workflow stub with full behavioral workflow</name>
  <files>workflows/research.md</files>
  <read_first>
    - workflows/research.md (current stub to replace)
    - workflows/new-milestone.md (canonical workflow pattern -- follow this structure)
    - commands/research.md (command file -- note WebFetch is available)
    - references/common-pitfalls.md (pitfalls catalog to cross-reference)
    - references/backtest-engine.md (engine pattern rules research should be aware of)
    - .planning/phases/03-strategy-specification/03-CONTEXT.md (decisions D-07 through D-11)
  </read_first>
  <action>
Replace the 11-line stub `workflows/research.md` with a complete behavioral workflow (~200-280 lines). Follow the structural pattern from `workflows/new-milestone.md`.

**Preamble: Sequence Validation**
- Read `.pmf/STATE.md` to get current phase N
- Verify discuss is done for phase N: STATE.md check (discuss step marked done) + file fallback (.pmf/phases/phase_N_discuss.md exists)
- If out of sequence, display [STOP] error per established pattern
- Research is OPTIONAL -- note this in the validation: "Research is recommended but optional. You can skip to /brrr:plan if your strategy is well-understood."

**Preamble: Context File Scan**
- Same pattern as new-milestone.md: check .pmf/context/ for new files, describe, confirm, record

**Step 1: Load Context**
- Read `.pmf/STRATEGY.md` for strategy type, asset, hypothesis
- Read `.pmf/phases/phase_N_discuss.md` for all strategy decisions
- Read `~/.pmf/references/common-pitfalls.md` for the pitfalls catalog
- Read `~/.pmf/references/backtest-engine.md` for engine pattern rules
- Check if `--deep` flag was passed

**Step 2: Research Recommendation**
- Per decision D-09: Auto-detect if research is needed based on discuss output
- Criteria for "recommended":
  - Strategy uses non-standard indicators or custom signals
  - Strategy combines 3+ indicators/signals
  - Strategy type is "custom"
  - Strategy involves multi-timeframe analysis
  - User mentioned unfamiliar concepts or academic references
- Criteria for "optional":
  - Classic well-known strategy (SMA crossover, RSI overbought/oversold, Bollinger Bands bounce)
  - Single-indicator strategy with standard parameters
  - Strategy type matches a textbook pattern
- Display recommendation: "Research is {recommended/optional} for your strategy. {reason}. Proceeding with research."
- Always proceed (the user already ran the command) -- the recommendation is informational

**Step 3: Knowledge Base Research (Claude's Training Data)**
- Per decision D-07: Training data first, web search to supplement
- Search Claude's training data for:
  1. Known implementations of this strategy type (describe 1-2 common approaches)
  2. Academic work or well-known papers related to the strategy concept
  3. Common parameter ranges used in published implementations
  4. Known failure modes specific to this strategy type
  5. Formalization alternatives (different ways to express the same trading idea)
- Present findings as they are discovered -- do not wait for all research to complete

**Step 4: Pitfall Cross-Reference**
- Per decision D-10: Warn about known lookahead traps specific to this strategy type
- Cross-reference the strategy's entry/exit logic against each pitfall in common-pitfalls.md
- For each relevant pitfall, explain HOW it could manifest in THIS specific strategy:
  - "Your RSI-based entry could have lookahead bias if the RSI is computed on the full dataset rather than history[:i+1]"
  - "Your mean-reversion strategy is particularly vulnerable to commission omission because it trades frequently"
- Rate each pitfall: HIGH RISK / MEDIUM RISK / LOW RISK for this specific strategy

**Step 5: Web Search (if --deep or if gaps found)**
- Standard mode (no --deep): Use WebFetch for 1-2 targeted searches if Claude's training data has gaps
- Deep mode (--deep): Per decision D-08:
  - Search 3-5 sources including academic papers, trading forums, and implementation guides
  - For each source: extract code patterns, compare approaches, rate reliability
  - Use WebFetch to access relevant pages
  - Search queries should be specific to the strategy type and asset class
- If no --deep and training data was sufficient, skip web search entirely
- Note: If WebFetch fails or returns unhelpful results, gracefully continue with training data findings only

**Step 6: Compile and Present Findings**
- Display research summary in structured format:
  ```
  --- Research Findings ---

  Strategy Type: {type}
  Research Depth: {standard | deep}

  ## Known Implementations
  {1-2 paragraph descriptions of common approaches}

  ## Academic/Published References
  {Papers, books, or well-known sources}

  ## Pitfall Assessment
  | Pitfall | Risk Level | How It Applies |
  |---------|------------|----------------|
  | Lookahead bias | {HIGH/MED/LOW} | {specific to this strategy} |
  | ... | ... | ... |

  ## Formalization Alternatives
  {Other ways to express this strategy that might be more robust}

  ## Recommendations for Plan Phase
  {Specific suggestions: parameter ranges, optimization method, data requirements}
  ```
- Ask: "Anything you want me to research further before we save this?"
- If user requests more, repeat relevant research steps

**Step 7: Write Output Artifact**
- Write `.pmf/phases/phase_N_research.md` with structure:
  ```markdown
  # Phase {N} -- Strategy Research

  ## Research Depth
  {standard | deep}

  ## Recommendation
  {Was research recommended or optional, and why}

  ## Known Implementations
  {Detailed findings}

  ## Academic References
  {Papers, books, established sources}

  ## Pitfall Assessment
  | Pitfall | Risk Level | Specific Risk |
  |---------|------------|---------------|
  {Each pitfall with risk level and specific explanation}

  ## Lookahead Trap Warnings
  {Specific warnings for this strategy type -- extracted from pitfall assessment for emphasis}

  ## Formalization Alternatives
  {Alternative approaches found}

  ## Recommendations for Plan Phase
  {Concrete suggestions for parameter space, optimization, data}

  ## Sources
  {List all sources used: training data topics, web URLs searched}

  ---
  *Phase {N} research completed: {date}*
  ```

**Step 8: Update STATE.md**
- Read `.pmf/STATE.md`
- Mark research step as done: `- [x] Research`
- Update Last Updated to today's date
- Append to History:
  ```
  ### {date} -- Phase {N} research completed
  - **Depth:** {standard | deep}
  - **Pitfalls identified:** {count} ({HIGH count} high risk)
  - **Sources:** {count}
  ```
- Write updated STATE.md

**Step 9: Confirmation**
- Display:
  ```
  Phase {N} research complete!

  Artifact: .pmf/phases/phase_{N}_research.md
  Pitfalls: {count} identified ({HIGH count} high risk)
  Sources: {count} consulted

  Next step: /brrr:plan
  ```

**Footer:**
```
---
*Workflow: research*
*Covers: RSCH-01, RSCH-02, RSCH-03, RSCH-04, RSCH-05*
```
  </action>
  <verify>
    <automated>test $(wc -l < workflows/research.md) -ge 150 && grep -q "phase_N_research" workflows/research.md && grep -q "\-\-deep" workflows/research.md && grep -q "common-pitfalls" workflows/research.md && grep -q "WebFetch" workflows/research.md && grep -q "RSCH-01" workflows/research.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File is at least 150 lines: `wc -l workflows/research.md` >= 150
    - Contains sequence validation: `grep -c "Sequence Validation" workflows/research.md` >= 1
    - Contains --deep flag handling: `grep -c "\-\-deep" workflows/research.md` >= 2
    - Cross-references common-pitfalls.md: `grep -c "common-pitfalls" workflows/research.md` >= 1
    - Uses WebFetch for web search: `grep -c "WebFetch" workflows/research.md` >= 1
    - Contains research recommendation logic: `grep -c "recommended\|optional" workflows/research.md` >= 2
    - Outputs phase_N_research.md: `grep -c "phase_N_research" workflows/research.md` >= 1
    - Contains pitfall risk rating: `grep -c "HIGH.*RISK\|MEDIUM.*RISK\|LOW.*RISK" workflows/research.md` >= 1 OR `grep -c "HIGH/MED/LOW" workflows/research.md` >= 1
    - Updates STATE.md: `grep -c "STATE.md" workflows/research.md` >= 3
    - Notes research is optional: `grep -ci "optional" workflows/research.md` >= 1
    - Footer lists covered requirements: `grep -c "RSCH-01" workflows/research.md` >= 1
  </acceptance_criteria>
  <done>
    workflows/research.md is a complete behavioral workflow (150+ lines) that Claude reads and follows to research the user's strategy type. It finds known implementations and academic references from training data, cross-references all 6 pitfalls from common-pitfalls.md with strategy-specific risk ratings, supports --deep mode with extended web search via WebFetch, auto-recommends whether research was needed, outputs phase_N_research.md, and updates STATE.md.
  </done>
</task>

</tasks>

<verification>
- `wc -l workflows/research.md` shows 150+ lines (was 11)
- `grep -c "## Preamble\|## Step" workflows/research.md` shows 7+ sections
- File follows structural pattern of workflows/new-milestone.md
</verification>

<success_criteria>
- The research workflow stub is replaced with a complete behavioral markdown workflow
- Standard mode uses Claude's training data + optional 1-2 web searches
- Deep mode searches 3-5 sources with detailed analysis per source
- Pitfalls from common-pitfalls.md are cross-referenced with strategy-specific risk ratings
- Research recommendation is auto-generated based on strategy complexity
- Output artifact phase_N_research.md has structured findings
- STATE.md is updated with research completion and history entry
</success_criteria>

<output>
After completion, create `.planning/phases/03-strategy-specification/03-02-SUMMARY.md`
</output>
