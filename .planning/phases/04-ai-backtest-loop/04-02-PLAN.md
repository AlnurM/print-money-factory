---
phase: 04-ai-backtest-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified: []
autonomous: false
requirements:
  - EXEC-01
  - EXEC-08
  - EXEC-09
  - EXEC-10
  - EXEC-11
  - DATA-01
  - DATA-02

must_haves:
  truths:
    - "User has observed the execute workflow running on a real or synthetic strategy"
    - "The terminal display matches the D-10 format with milestone header and iteration blocks"
    - "Stop condition triggered and displayed correctly"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete execute workflow by running /brrr:execute with a test strategy milestone.

Purpose: The execute workflow is behavioral markdown -- the only way to verify it works is to run it end-to-end. This checkpoint confirms the workflow produces correct output before the phase is marked complete.

Output: Human confirmation that the loop works
</objective>

<execution_context>
@/Users/alikeforalike/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alikeforalike/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-ai-backtest-loop/04-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end smoke test of /brrr:execute</name>
  <files>workflows/execute.md</files>
  <read_first>
    - .planning/phases/04-ai-backtest-loop/04-01-SUMMARY.md
    - workflows/execute.md
  </read_first>
  <action>
Present the verification instructions to the user. The user will run the /brrr:execute command with a test strategy and verify the workflow behavior end-to-end.

Steps for the user:
1. Create a test milestone if none active: /brrr:new-milestone with a simple SMA crossover strategy on BTC/USDT daily or SPY daily
2. Run /brrr:discuss --auto and /brrr:plan accepting defaults
3. Run /brrr:execute --iterations 3
4. Verify terminal display, artifact creation, stop condition, and STATE.md update
5. Optionally test --resume flag

Wait for user to report "approved" or describe issues found.
  </action>
  <verify>
    <automated>echo "Checkpoint: requires human verification of /brrr:execute behavior"</automated>
  </verify>
  <acceptance_criteria>
    - Terminal display shows iteration blocks with params and IS/OOS metrics
    - Per-iteration artifacts (params.json, metrics.json, verdict.json, equity.png) exist in phase_N_execute/ directory
    - phase_N_best_result.json exists after completion
    - Stop condition (MINT/PLATEAU/REKT/NO DATA) displayed correctly
    - STATE.md updated with execute marked done
    - "brrr..." appears in terminal output
    - AI analysis uses trading-domain language
  </acceptance_criteria>
  <done>User has run /brrr:execute, observed at least one full iteration with formatted output, verified artifacts exist on disk, and approved the workflow behavior</done>
</task>

</tasks>

<verification>
Human confirms the execute workflow runs correctly end-to-end.
</verification>

<success_criteria>
- User has run /brrr:execute and seen at least one full iteration with formatted output
- Artifacts (JSON, PNG) exist on disk
- Stop condition triggered and reported correctly
- User approves the workflow behavior
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-backtest-loop/04-02-SUMMARY.md`
</output>
