---
phase: 09-bayesian-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/plan.md
autonomous: true
requirements: [OPT-04]
must_haves:
  truths:
    - "Plan workflow offers bayesian as a fourth optimization method"
    - "Auto-selection recommends bayesian when total_combinations > 500"
    - "Override prompt includes bayesian option"
    - "Plan artifact includes optimization_method: bayesian and sampler: auto fields when bayesian selected"
  artifacts:
    - path: "workflows/plan.md"
      provides: "Bayesian optimization method in Step 4"
      contains: "bayesian"
  key_links:
    - from: "workflows/plan.md"
      to: "workflows/execute.md"
      via: "optimization_method field in plan artifact"
      pattern: "optimization_method.*bayesian"
---

<objective>
Add Bayesian optimization as a fourth method in the plan workflow's Step 4.

Purpose: Per D-05/D-06/D-07/D-08, users need to see Bayesian as an optimization option in `/brrr:plan`. The auto-selection rule changes: >500 combinations auto-selects bayesian instead of random. The plan artifact gets new fields when bayesian is chosen.

Output: Updated `workflows/plan.md` with bayesian method in Step 4.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-bayesian-optimization/09-CONTEXT.md

@workflows/plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update plan.md Step 4 with Bayesian optimization method</name>
  <files>workflows/plan.md</files>
  <read_first>
    - workflows/plan.md (full file -- understand current Step 4 structure at line 253)
    - .planning/phases/09-bayesian-optimization/09-CONTEXT.md (decisions D-05 through D-08)
  </read_first>
  <action>
    Edit `workflows/plan.md` Step 4 (starts at line 253) to add Bayesian optimization. Make these specific changes:

    **1. Update Auto-Selection Rules (line 258-261):**

    Replace the current three rules with four rules per D-06:
    ```
    - **Total combinations <= 100:** Grid search (exhaustive) -- tests every single combination.
    - **Total combinations 101-500:** Random search -- samples N iterations (default: 50).
    - **Total combinations > 500:** Bayesian optimization (Optuna TPE/CMA-ES) -- uses probability model to suggest promising parameter combinations. Far more efficient than random for large spaces.
    - **Walk-forward:** Available as user override for any parameter space size. Best for robustness testing with rolling train/test windows.
    ```

    Note: The old rule "3+ free parameters OR total combinations > 10000: Walk-forward" is replaced. Walk-forward remains available via override but is no longer auto-selected. Per D-06, bayesian is strictly better for parameter optimization on large spaces.

    **2. Update Display and Override section (line 265-276):**

    Update the method display to include bayesian:
    ```
    Method: {grid_search / random_search / bayesian / walk_forward}
    ```

    Add bayesian-specific display line:
    ```
    {If bayesian: "Sampler: auto (TPE or CMA-ES selected at execute time based on parameter types)"}
    ```

    Update override prompt per D-08:
    ```
    Override? (grid / random / walk-forward / bayesian / keep)
    ```

    **3. Add plan artifact fields for bayesian (after line 281):**

    Add a new subsection after the override handling:
    ```markdown
    **When bayesian is selected (per D-07):**
    Record these additional fields in the plan artifact:
    - `optimization_method: bayesian`
    - `sampler: auto` (sampler auto-selects TPE vs CMA-ES at execute time based on parameter types -- per D-10)
    - `min_iterations: 20` (bayesian needs at least 2x warmup trials to be effective; warmup is 10 random trials)

    **Important:** If the user sets `--iterations` below 20 with bayesian method, warn: "Bayesian optimization needs at least 20 iterations (10 warmup + 10 guided). With fewer iterations, random search would be equally effective. Continue anyway? (yes / switch to random)"
    ```

    **4. Update the grid search warning (line 281):**
    Keep the existing grid search warning for >10000 combinations. No change needed here.

    Do NOT change any other steps in plan.md. Only modify Step 4.
  </action>
  <verify>
    <automated>grep -n "bayesian" /Users/alikeforalike/Documents/Dev/print-money-factory/workflows/plan.md | head -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bayesian" workflows/plan.md` returns >= 5 (method name appears in rules, display, override, artifact fields)
    - `grep "Total combinations > 500" workflows/plan.md` finds the new auto-selection rule
    - `grep "grid / random / walk-forward / bayesian / keep" workflows/plan.md` finds the updated override prompt (per D-08)
    - `grep "optimization_method: bayesian" workflows/plan.md` finds the plan artifact field (per D-07)
    - `grep "sampler: auto" workflows/plan.md` finds the sampler field (per D-07/D-10)
    - `grep "min_iterations: 20" workflows/plan.md` finds the minimum iterations field
    - The old rule "3+ free parameters OR total combinations > 10000" for walk-forward auto-selection is REMOVED
    - Walk-forward still mentioned as override option
  </acceptance_criteria>
  <done>Step 4 of plan.md includes bayesian as fourth method with correct auto-selection (>500), override prompt, and plan artifact fields</done>
</task>

</tasks>

<verification>
```bash
# Verify bayesian appears in auto-selection, display, override, and artifact sections
grep -c "bayesian" workflows/plan.md  # >= 5
# Verify old walk-forward auto-selection rule is gone
grep "3+ free parameters" workflows/plan.md  # should return nothing
# Verify override prompt has all 4 options
grep "grid / random / walk-forward / bayesian / keep" workflows/plan.md
```
</verification>

<success_criteria>
- Bayesian appears as fourth optimization method in Step 4 auto-selection rules
- Auto-selection: >500 combinations triggers bayesian (per D-06)
- Override prompt includes bayesian (per D-08)
- Plan artifact fields documented: optimization_method, sampler, min_iterations (per D-07)
- Walk-forward still available via override but not auto-selected for parameter optimization
- No other steps in plan.md are modified
</success_criteria>

<output>
After completion, create `.planning/phases/09-bayesian-optimization/09-02-SUMMARY.md`
</output>
