---
phase: 09-bayesian-optimization
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - references/optuna_bridge.py
  - references/test_optuna_bridge.py
autonomous: true
requirements: [OPT-01, OPT-02, OPT-03]
must_haves:
  truths:
    - "optuna_bridge.py creates a study, suggests params via ask(), reports via tell()"
    - "CMA-ES sampler auto-selected when all params are continuous floats without step"
    - "TPE sampler auto-selected when categorical or int params are present"
    - "Study persists to SQLite and loads correctly on resume"
    - "CMA-ES sampler state pickled and restored on resume"
    - "Param space changes detected between resume runs"
    - "Composite score combines Sharpe with capped drawdown penalty"
    - "Warmup vs guided mode correctly reported based on completed trial count"
  artifacts:
    - path: "references/optuna_bridge.py"
      provides: "Optuna study lifecycle, sampler selection, Ask-and-Tell, composite score, resume"
      exports: ["build_distributions", "select_sampler", "get_or_create_study", "suggest_params", "report_result", "compute_composite_score", "is_warmup", "detect_param_changes", "save_sampler", "load_sampler"]
    - path: "references/test_optuna_bridge.py"
      provides: "Unit tests for all optuna_bridge functions"
      min_lines: 100
  key_links:
    - from: "references/optuna_bridge.py"
      to: "optuna"
      via: "import optuna"
      pattern: "optuna\\.create_study|study\\.ask|study\\.tell"
    - from: "references/test_optuna_bridge.py"
      to: "references/optuna_bridge.py"
      via: "import from optuna_bridge"
      pattern: "from optuna_bridge import"
---

<objective>
Create the `optuna_bridge.py` module with full test coverage using TDD.

Purpose: This module encapsulates all Optuna integration logic -- study lifecycle, sampler auto-selection (TPE vs CMA-ES per D-09), Ask-and-Tell parameter suggestion (per D-01), composite scoring (per D-04), SQLite persistence (per D-11/D-12), CMA-ES pickle for resume, and param space change detection (per D-13). It will be consumed by the execute workflow in Plan 03.

Output: A tested `references/optuna_bridge.py` module and `references/test_optuna_bridge.py` test file.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-bayesian-optimization/09-CONTEXT.md
@.planning/phases/09-bayesian-optimization/09-RESEARCH.md

@references/metrics.py
@references/backtest_engine.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for optuna_bridge.py</name>
  <files>references/test_optuna_bridge.py</files>
  <read_first>
    - references/metrics.py (see existing test patterns in references/)
    - references/test_metrics.py (see existing test patterns)
    - .planning/phases/09-bayesian-optimization/09-RESEARCH.md (code examples and patterns)
  </read_first>
  <behavior>
    - test_build_distributions_int: IntDistribution(5, 50, step=1) from {"name": "fast_period", "type": "int", "min": 5, "max": 50, "step": 1}
    - test_build_distributions_float: FloatDistribution(1.0, 3.0) from {"name": "atr_mult", "type": "float", "min": 1.0, "max": 3.0}
    - test_build_distributions_float_with_step: FloatDistribution(0.1, 1.0, step=0.1) from {"name": "threshold", "type": "float", "min": 0.1, "max": 1.0, "step": 0.1}
    - test_build_distributions_categorical: CategoricalDistribution(["ema", "sma"]) from {"name": "ma_type", "type": "categorical", "choices": ["ema", "sma"]}
    - test_select_sampler_cmaes: Returns CmaEsSampler when all params are float without step AND len >= 2
    - test_select_sampler_tpe_categorical: Returns TPESampler when categorical param present
    - test_select_sampler_tpe_int: Returns TPESampler when int param present
    - test_select_sampler_tpe_float_with_step: Returns TPESampler when float has step constraint
    - test_select_sampler_tpe_single_float: Returns TPESampler when only 1 float param (CMA-ES needs >= 2)
    - test_ask_tell_lifecycle: study.ask() returns trial with params, study.tell() records score, study has 1 completed trial
    - test_composite_score_basic: sharpe=2.0, max_dd=-10%, dd_target=15% -> score=2.0 (no penalty)
    - test_composite_score_penalty: sharpe=2.0, max_dd=-25%, dd_target=15% -> score=2.0 - min(10*2, 5) = -3.0 (penalty capped)
    - test_composite_score_no_trades: trade_count=0 -> -inf
    - test_composite_score_penalty_cap: sharpe=2.5, max_dd=-40%, dd_target=15% -> score=2.5-5.0=-2.5 (penalty capped at 5)
    - test_is_warmup_true: completed < n_startup_trials -> True
    - test_is_warmup_false: completed >= n_startup_trials -> False
    - test_sqlite_persistence: Create study, add trial, reload study from same SQLite, verify trial exists
    - test_param_change_detection_no_changes: Same distributions -> empty list
    - test_param_change_detection_new_param: New param added -> ["NEW param: x"]
    - test_param_change_detection_changed_range: Range changed -> ["CHANGED: x"]
    - test_param_change_detection_removed_param: Param removed -> ["REMOVED: x"]
    - test_save_load_sampler: Pickle CMA-ES sampler, load it back, verify type matches
  </behavior>
  <action>
    Create `references/test_optuna_bridge.py` with all tests listed in behavior section.

    Use `sys.path.insert(0, os.path.dirname(__file__))` at top to enable `from optuna_bridge import ...`.

    Use `tmp_path` pytest fixture for SQLite database paths (avoids test pollution).

    Import all functions from optuna_bridge: build_distributions, select_sampler, get_or_create_study, suggest_params, report_result, compute_composite_score, is_warmup, detect_param_changes, save_sampler, load_sampler.

    For sampler type assertions, use `isinstance(sampler, optuna.samplers.CmaEsSampler)` and `isinstance(sampler, optuna.samplers.TPESampler)`.

    For the ask/tell lifecycle test, create a study with TPESampler, call suggest_params, then report_result with a score, and verify study.trials has 1 completed trial.

    For composite score tests, pass dicts with keys: sharpe_ratio, max_drawdown, trade_count. The dd_target is a float (e.g., 0.15 for 15%).

    For SQLite persistence test, create a study at tmp_path, do ask+tell, then create a NEW study object with same storage path and load_if_exists=True, verify len(study.trials) == 1.

    All tests MUST fail at this point since optuna_bridge.py does not exist yet.
  </action>
  <verify>
    <automated>cd /Users/alikeforalike/Documents/Dev/print-money-factory/references && ~/.pmf/venv/bin/python -m pytest test_optuna_bridge.py -x --tb=short 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File `references/test_optuna_bridge.py` exists
    - Contains at least 20 test functions (grep -c "def test_" references/test_optuna_bridge.py returns >= 20)
    - All tests fail with ImportError since optuna_bridge.py does not exist yet
  </acceptance_criteria>
  <done>Test file exists with 20+ test functions, all failing with ImportError</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement optuna_bridge.py to pass all tests</name>
  <files>references/optuna_bridge.py</files>
  <read_first>
    - references/test_optuna_bridge.py (the tests that must pass)
    - .planning/phases/09-bayesian-optimization/09-RESEARCH.md (complete code example in "Complete optuna_bridge.py Structure" section)
  </read_first>
  <action>
    Create `references/optuna_bridge.py` implementing all functions to pass the tests from Task 1.

    Follow the complete structure from the research doc's "Complete optuna_bridge.py Structure" section with these specific implementations:

    1. `build_distributions(param_space)` -- Convert plan param space list[dict] to Optuna distributions dict. Handle "int" (IntDistribution), "float" (FloatDistribution with optional step and log), "categorical" (CategoricalDistribution). Per D-01, this is the define-and-run approach.

    2. `select_sampler(param_space, seed=42)` -- Per D-09: CMA-ES when ALL params are type=="float" with step==None AND len(param_space)>=2. Otherwise TPE with multivariate=True. Both use n_startup_trials=10. Return tuple (sampler, sampler_name_string).

    3. `get_or_create_study(study_name, storage_dir, sampler, direction="maximize")` -- Per D-11: SQLite at storage_dir/optuna_study.db. Use load_if_exists=True for resume support.

    4. `suggest_params(study, distributions)` -- Per D-01: Call study.ask(distributions), return (trial, trial.params).

    5. `report_result(study, trial, value)` -- Call study.tell(trial, value).

    6. `compute_composite_score(metrics, dd_target)` -- Per D-04: score = sharpe - dd_penalty. Penalty = min(max(0, abs(max_dd) - abs(dd_target)) * 2.0, 5.0). Return -inf if trade_count==0. The dd_target is a float like 0.15 for 15%.

    7. `is_warmup(study, n_startup_trials=10)` -- Per D-03: Count completed trials, return completed < n_startup_trials.

    8. `detect_param_changes(study, new_distributions)` -- Per D-13: Compare new distributions against last trial's distributions. Return list of change descriptions.

    9. `save_sampler(study, storage_dir)` -- Pickle study.sampler to storage_dir/optuna_sampler.pkl.

    10. `load_sampler(storage_dir)` -- Load pickled sampler from storage_dir/optuna_sampler.pkl. Return None if file doesn't exist.

    Add `optuna.logging.set_verbosity(optuna.logging.WARNING)` at module level to suppress verbose output.

    Add module docstring explaining this is the Optuna bridge for PMF backtest loop.
  </action>
  <verify>
    <automated>cd /Users/alikeforalike/Documents/Dev/print-money-factory/references && ~/.pmf/venv/bin/python -m pytest test_optuna_bridge.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `references/optuna_bridge.py` exists
    - All 20+ tests pass (grep "passed" in pytest output)
    - Module exports all 10 functions: build_distributions, select_sampler, get_or_create_study, suggest_params, report_result, compute_composite_score, is_warmup, detect_param_changes, save_sampler, load_sampler
    - `grep -c "def " references/optuna_bridge.py` returns >= 10
    - `grep "optuna.logging.set_verbosity" references/optuna_bridge.py` finds the line
  </acceptance_criteria>
  <done>All tests pass, optuna_bridge.py exports 10 functions, module suppresses verbose Optuna logging</done>
</task>

</tasks>

<verification>
Run full test suite:
```bash
cd /Users/alikeforalike/Documents/Dev/print-money-factory/references && ~/.pmf/venv/bin/python -m pytest test_optuna_bridge.py -v
```
All tests pass. Module can be imported without errors.
</verification>

<success_criteria>
- references/optuna_bridge.py exists with 10 exported functions
- references/test_optuna_bridge.py exists with 20+ test functions
- All tests pass
- Ask-and-Tell lifecycle works (study.ask -> trial.params -> study.tell)
- CMA-ES auto-selected for all-float-no-step params, TPE otherwise
- SQLite persistence creates .db file and loads on resume
- Composite score matches D-04 formula with 5.0 penalty cap
- Warmup detection correct based on completed trial count
</success_criteria>

<output>
After completion, create `.planning/phases/09-bayesian-optimization/09-01-SUMMARY.md`
</output>
