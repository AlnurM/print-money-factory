---
phase: 05-verify-export
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - bin/install.mjs
autonomous: false
requirements: [VRFY-01, VRFY-02, VRFY-03, VRFY-04, VRFY-05, VRFY-06, VRFY-07, VRFY-08, VRFY-09, VRFY-10, VRFY-11, EXPT-01, EXPT-02, EXPT-03, EXPT-04, EXPT-05, EXPT-06, EXPT-07]

must_haves:
  truths:
    - "Install script copies report_generator.py and pinescript-rules.md to ~/.pmf/"
    - "User can run /brrr:verify and get a working HTML report in their browser"
    - "Export package contains all expected files"
  artifacts:
    - path: "bin/install.mjs"
      provides: "Updated install script that copies new Phase 5 files"
      contains: "report_generator"
  key_links:
    - from: "bin/install.mjs"
      to: "references/report_generator.py"
      via: "file copy during install"
      pattern: "report_generator"
---

<objective>
Update the install script to copy the new Phase 5 files (report_generator.py, pinescript-rules.md) to ~/.pmf/, then perform end-to-end human verification of the complete /brrr:verify flow.

Purpose: The install script must be updated so that new installs get the report generator and PineScript rules. Then a human smoke test confirms the entire pipeline works: report generation, AI assessment, and at minimum one of the approval/debug paths.

Output: Updated install script + human-verified /brrr:verify workflow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-verify-export/05-CONTEXT.md
@.planning/phases/05-verify-export/05-01-SUMMARY.md
@.planning/phases/05-verify-export/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update install script to copy Phase 5 files</name>
  <files>bin/install.mjs</files>
  <read_first>
    - bin/install.mjs (current install script -- understand how it copies files to ~/.pmf/)
    - references/report_generator.py (new file that must be copied)
    - references/pinescript-rules.md (new file that must be copied)
  </read_first>
  <action>
Update bin/install.mjs to include the two new Phase 5 reference files in the copy list:

1. Add `references/report_generator.py` to the list of reference files copied to `~/.pmf/references/`
2. Add `references/pinescript-rules.md` to the list of reference files copied to `~/.pmf/references/`

Follow the exact same copy pattern already used for other reference files (metrics.py, backtest_engine.py, data_sources.py). The install script already copies the references/ directory -- just ensure these two new files are included in the copy.

Also verify that `templates/report-template.html` is already being copied (it should be from Phase 1). If not, add it to the templates copy list.

After updating, run the install script locally to verify the new files appear in ~/.pmf/.
  </action>
  <verify>
    <automated>grep -cE 'report_generator|pinescript-rules' bin/install.mjs && ls ~/.pmf/references/report_generator.py ~/.pmf/references/pinescript-rules.md && echo 'OK'</automated>
  </verify>
  <acceptance_criteria>
    - bin/install.mjs contains "report_generator" in its file copy list or directory copy logic
    - bin/install.mjs contains "pinescript-rules" in its file copy list or directory copy logic
    - After running install: ~/.pmf/references/report_generator.py exists
    - After running install: ~/.pmf/references/pinescript-rules.md exists
    - After running install: ~/.pmf/templates/report-template.html exists
    - After running install: ~/.pmf/workflows/verify.md exists and is NOT the 10-line stub
  </acceptance_criteria>
  <done>Install script copies all Phase 5 files to ~/.pmf/ and local install completes successfully</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end smoke test of /brrr:verify</name>
  <files>workflows/verify.md</files>
  <read_first>
    - .planning/phases/05-verify-export/05-01-SUMMARY.md
    - .planning/phases/05-verify-export/05-02-SUMMARY.md
    - workflows/verify.md
  </read_first>
  <action>
Present the verification instructions to the user. The user will run /brrr:verify with an active milestone that has a completed execute phase and verify the complete workflow behavior.

Steps for the user:
1. Ensure an active milestone with completed execute phase exists (if not, run /brrr:new-milestone with a simple SMA crossover on BTC/USDT or SPY, then /brrr:discuss --auto, /brrr:plan, /brrr:execute --iterations 3)
2. Run /brrr:verify (no flags)
3. Open the generated HTML report in browser and check all sections
4. Read the AI assessment for specificity
5. Choose --approved or --debug to test one complete path
6. If --approved: verify output/ directory has all 7 export files
7. If --debug: verify diagnosis document and STATE.md phase increment

Wait for user to report "approved" or describe issues found.
  </action>
  <verify>
    <automated>echo "Checkpoint: requires human verification of /brrr:verify behavior"</automated>
  </verify>
  <acceptance_criteria>
    - HTML report opens in browser with interactive plotly charts
    - Metrics summary cards appear at top with target-based color coding
    - Equity curve shows both Strategy and Buy & Hold lines
    - Drawdown chart has max-DD horizontal line
    - Iteration table shows all iterations with Sharpe evolution
    - Trade log has green/red P&L coloring
    - AI assessment references specific metrics and compares to targets
    - At least one path (approved/debug) completes successfully
    - If approved: output/ directory contains 7 files including 2 PineScript files
    - If debug: diagnosis document exists and STATE.md shows new phase cycle
  </acceptance_criteria>
  <done>User has run /brrr:verify, confirmed the HTML report renders correctly in browser, verified the AI assessment is specific, and confirmed at least one complete path (approved or debug) works end-to-end</done>
</task>

</tasks>

<verification>
- Install script includes all Phase 5 files in copy operations
- /brrr:verify generates a working HTML report that opens in browser
- Report contains all required sections per D-05
- AI assessment references specific metrics and targets
- At least one path (approved/debug) verified end-to-end
</verification>

<success_criteria>
- Human confirms HTML report opens correctly in browser with interactive charts
- Human confirms AI assessment is specific and useful (not generic)
- Human confirms either export package or debug cycle works correctly
- Phase 5 complete -- the final phase of the product pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/05-verify-export/05-03-SUMMARY.md`
</output>
