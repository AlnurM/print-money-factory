---
phase: 10-enhanced-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/verify.md
autonomous: true
requirements:
  - EXPT-08

must_haves:
  truths:
    - "Running /brrr:verify --approved generates bot-building-guide.md in the output directory"
    - "The guide contains platform-specific instructions matching the strategy's asset class"
    - "The guide uses actual strategy parameters from best_result.json, not generic placeholders"
    - "The guide is only generated during --approved flow, not --debug"
    - "The export summary displays 8 files instead of 7"
  artifacts:
    - path: "workflows/verify.md"
      provides: "Step 5a.8 bot-building-guide generation and updated Step 5a.9 export summary"
      contains: "### 5a.8: Generate bot-building-guide.md (EXPT-08)"
  key_links:
    - from: "workflows/verify.md Step 5a.8"
      to: ".pmf/STRATEGY.md"
      via: "AI reads STRATEGY.md for asset class detection"
      pattern: "STRATEGY.md.*asset class"
    - from: "workflows/verify.md Step 5a.8"
      to: ".pmf/phases/phase_N_best_result.json"
      via: "AI reads best_result.json for concrete parameter values"
      pattern: "best_result.json"
    - from: "workflows/verify.md Step 5a.9"
      to: "bot-building-guide.md"
      via: "Export summary lists bot-building-guide.md as 8th file"
      pattern: "bot-building-guide.md"
---

<objective>
Add bot-building-guide.md generation to the /brrr:verify --approved export pipeline. The guide provides platform-specific step-by-step instructions for deploying an approved strategy to live trading, with content auto-detected from the strategy's asset class.

Purpose: Users who get a strategy approved need actionable deployment instructions for their specific platform (crypto exchange, stock broker, or forex platform), not generic advice.
Output: Updated workflows/verify.md with new Step 5a.8 (guide generation) and renumbered Step 5a.9 (export summary showing 8 files).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-enhanced-export/10-CONTEXT.md
@.planning/phases/10-enhanced-export/10-RESEARCH.md
@workflows/verify.md

<interfaces>
<!-- Key structure from verify.md that the executor needs -->

Current Step 5a export flow (lines 354-808):
  5a.1: Create output directory
  5a.2: Generate PineScript files (EXPT-01)
  5a.3: Generate trading-rules.md (EXPT-02) -- TONE MODEL for bot-building-guide
  5a.4: Generate performance-report.md (EXPT-03)
  5a.5: Generate backtest_final.py (EXPT-04)
  5a.6: Generate live-checklist.md (EXPT-05)
  5a.7: Copy report HTML (EXPT-06)
  5a.8: Display export summary (EXPT-07) -- RENUMBER to 5a.9, update count

Step 5a.3 pattern (the model to follow):
- Reads: .pmf/STRATEGY.md, .pmf/phases/phase_N_discuss.md, .pmf/phases/phase_N_best_result.json
- Writes: .pmf/output/trading-rules.md
- Uses practitioner tone, concrete parameters from best_result.json

Step 5a.8 export summary (line 792-808):
- Lists 7 files with descriptions
- Says "Export package generated in output/ with {N} files."

Appendix requirement coverage table (lines 1100-1116):
- Maps each EXPT-XX to its step
- Needs EXPT-08 row added
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 5a.8 bot-building-guide.md generation to verify.md</name>
  <files>workflows/verify.md</files>
  <read_first>
    - workflows/verify.md (lines 460-530 for trading-rules.md pattern; lines 784-808 for export summary; lines 1100-1117 for appendix)
    - .planning/phases/10-enhanced-export/10-RESEARCH.md (platform-specific content, code patterns, detection logic)
    - .planning/phases/10-enhanced-export/10-CONTEXT.md (locked decisions D-01 through D-09)
  </read_first>
  <action>
Insert a new Step 5a.8 between the current Step 5a.7 (Copy Report HTML, line 791) and the current Step 5a.8 (Display Export Summary, line 792). Then renumber the old 5a.8 to 5a.9 and update it.

**New Step 5a.8: Generate bot-building-guide.md (EXPT-08)**

Follow the exact same generation pattern as Step 5a.3 (trading-rules.md). The step must instruct the AI to:

1. Read strategy context:
   - `.pmf/STRATEGY.md` for asset class, exchange/source, timeframe
   - `.pmf/phases/phase_N_discuss.md` for strategy logic details
   - `.pmf/phases/phase_N_best_result.json` for optimized parameter values
   - `.pmf/output/trading-rules.md` for cross-reference with entry/exit rules

2. Detect platform from STRATEGY.md (per D-04, D-05):
   - Crypto assets (BTC, ETH, SOL, pairs with USDT/BTC, or exchange names like Binance/Bybit) -> Exchange API section using ccxt
   - Stock assets (ticker symbols like SPY, AAPL, TSLA, or broker names like Alpaca/IBKR) -> Broker API section (Alpaca, Interactive Brokers)
   - Forex assets (pairs like EUR/USD, GBP/JPY, or "forex" in strategy type) -> MT5/OANDA section
   - Ambiguous -> Include brief sections for all applicable platforms (per D-06)

3. Write `.pmf/output/bot-building-guide.md` with this structure (per D-01):

The template in the workflow step must show all 7 sections with guidance for what AI should generate in each. Use the guide output template from 10-RESEARCH.md (lines 440-482) as the base structure. Include these specifics:

**Risk Warning** (per D-09): Start with the exact text: "This guide assumes you've validated the strategy in backtesting. Past performance does not guarantee future results. Start with paper trading or minimal position sizes."

**Section 1: Prerequisites** - Platform-specific requirements (exchange account, API keys, Python packages). Per D-02, use actual asset/exchange from STRATEGY.md.

**Section 2: Platform Setup** - Step-by-step API setup. Include code patterns (per D-03, pattern level only, not full bot) for:
  - Crypto: ccxt connection pattern with `enableRateLimit: True`, env vars for keys
  - Stocks: Alpaca TradingClient or ib_async IB connection pattern (NOTE: use ib_async NOT ib_insync -- ib_insync is archived)
  - Forex: oandapyV20 API client or MT5 initialize pattern (note MT5 is Windows only)

**Section 3: Strategy Configuration** - Map best_result.json parameters to live config. Per D-02, use actual parameter values, not `{placeholder}` syntax.

**Section 4: Order Types & Execution** - Which order types for this strategy's entry/exit style. Limit vs market based on timeframe. Slippage handling.

**Section 5: Risk Management** - Position sizing formula using actual stop loss from best_result.json. Circuit breakers: daily loss limit (3-5%), consecutive loss limit (5), max drawdown kill switch referencing backtest max drawdown.

**Section 6: Monitoring & Alerts** - What to watch (execution, position status, equity, API health, strategy drift). Include a minimal Telegram/Discord webhook notification pattern (5-10 lines) as concrete example.

**Section 7: Go-Live Checklist** - Strategy-specific pre-launch verification. Distinct from the generic live-checklist.md.

Per D-07: Target audience is an experienced trader who knows their platform. Per D-08: Use direct second-person language throughout.

Include inline platform-specific pitfalls within relevant sections (e.g., "OANDA uses underscores not slashes: EUR_USD not EUR/USD") rather than a separate pitfalls subsection.

Include a brief note about TradingView-based automation: "For TradingView-based automation, use the exported PineScript indicator with TradingView alerts connected to your broker's webhook endpoint." One paragraph in the Platform Setup section, not a full section.

**Renumber old Step 5a.8 to Step 5a.9** and update:
- Change heading from `### 5a.8: Display Export Summary (EXPT-07)` to `### 5a.9: Display Export Summary (EXPT-07)`
- Update the file count from 7 to 8
- Add `bot-building-guide.md` to the file listing with description: `Bot deployment guide for live trading`
- The listing should show 8 files total

**Update Appendix** (around line 1100):
- Add row: `| EXPT-08 | Step 5a.8 | bot-building-guide.md with platform-specific deployment instructions |`
- Update EXPT-07 row to reference Step 5a.9 instead of Step 5a.8
  </action>
  <verify>
    <automated>grep -n "5a.8: Generate bot-building-guide.md" workflows/verify.md && grep -n "5a.9: Display Export Summary" workflows/verify.md && grep -c "bot-building-guide" workflows/verify.md | xargs test 3 -le</automated>
  </verify>
  <acceptance_criteria>
    - `grep "### 5a.8: Generate bot-building-guide.md (EXPT-08)" workflows/verify.md` matches exactly 1 line
    - `grep "### 5a.9: Display Export Summary (EXPT-07)" workflows/verify.md` matches exactly 1 line
    - `grep "bot-building-guide.md" workflows/verify.md` matches at least 3 times (step heading, file listing, appendix)
    - `grep "EXPT-08" workflows/verify.md` matches at least 2 times (step heading, appendix row)
    - `grep "8 files" workflows/verify.md` or `grep "{N} files" workflows/verify.md` shows updated count
    - `grep "STRATEGY.md" workflows/verify.md` appears in the new step (asset class detection)
    - `grep "best_result.json" workflows/verify.md` appears in the new step (concrete parameters)
    - `grep "risk warning\|Risk Warning\|Past performance" workflows/verify.md` appears in the new step
    - `grep "ib_async" workflows/verify.md` appears (NOT ib_insync)
    - `grep "ccxt\|oandapyV20\|alpaca" workflows/verify.md` appears in code patterns
    - No remaining references to old "5a.8: Display Export Summary" (all renumbered to 5a.9)
    - The section for Step 5a.8 appears between Step 5a.7 and Step 5a.9 in the file
  </acceptance_criteria>
  <done>
    workflows/verify.md contains Step 5a.8 that generates bot-building-guide.md with platform detection from STRATEGY.md, all 7 guide sections per D-01, concrete parameters from best_result.json per D-02, code patterns per D-03, risk warning per D-09, and practitioner tone per D-07/D-08. The old export summary is renumbered to 5a.9 showing 8 files. The appendix includes the EXPT-08 row.
  </done>
</task>

</tasks>

<verification>
After task completion, verify the full integration:

1. Step ordering is correct: 5a.1 through 5a.9 in sequence, no gaps or duplicates
2. The new step follows the same pattern as 5a.3 (read context, generate markdown, write file)
3. All locked decisions D-01 through D-09 are reflected in the step content
4. The export summary in 5a.9 lists 8 files including bot-building-guide.md
5. The appendix requirement coverage table includes EXPT-08
6. No references to deprecated libraries (ib_insync should be ib_async)
</verification>

<success_criteria>
- workflows/verify.md has 9 sub-steps in Step 5a (5a.1 through 5a.9)
- Step 5a.8 generates bot-building-guide.md with platform-specific content
- Platform detection reads STRATEGY.md asset class automatically
- Guide template includes all 7 sections: Prerequisites, Platform Setup, Strategy Configuration, Order Types & Execution, Risk Management, Monitoring & Alerts, Go-Live Checklist
- Concrete strategy parameters used (not generic placeholders)
- Export summary counts 8 files
- EXPT-08 traced in appendix
</success_criteria>

<output>
After completion, create `.planning/phases/10-enhanced-export/10-01-SUMMARY.md`
</output>
